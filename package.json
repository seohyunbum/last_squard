{
  "name": "last-squad",
  "private": true,
  "version": "1.0.0",
  "description": "라스트워 스타일 레인 러너 — 게이트로 병력을 불려 고철 로봇 군단을 막는다",
  "scripts": {
    "start": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "balance": "node scripts/balance-check.js",
    "smoke": "node scripts/browser-smoke.js",
    "verify": "npm run test && npm run balance",
    "verify:full": "npm run verify && npm run smoke && npm run build",
    "build": "node scripts/build-single.js"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
